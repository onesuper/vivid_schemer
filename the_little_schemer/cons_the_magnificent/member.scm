(begin

(define member?
  (lambda (a lat)
    (cond
      ((null? lat) #f)
      (else (or (eq? (car lat) a)
                (member? a (cdr lat)))))))

(member? (quote eggs) (quote (scrambled eggs with tomatoes)))

)